# dotfiles

Конфигурационные файлы для Claude Code.

## Установка

```bash
git clone git@github.com:mvladt/dotfiles.git ~/dotfiles

ln -sf ~/dotfiles/.claude/settings.json ~/.claude/settings.json
ln -sf ~/dotfiles/.claude/skills ~/.claude/skills
```
