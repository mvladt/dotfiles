---
description: Правила для frontend-проектов (Vue, React и т.д.)
globs: "**/*.vue,**/*.tsx,**/*.jsx,**/vite.config.*,**/playwright.config.*"
---

# Frontend

- **Нативный CSS вместо препроцессоров** — использовать обычный CSS (`<style scoped>`), SCSS/SASS только если нативный CSS объективно не справляется с задачей
- **E2E тесты** — smoke-тест на уровне браузера: проверяем, что ключевые флоу работают от начала до конца. Детали — на уровне unit/integration
- **Адаптивный дизайн (mobile-first)** — приложение должно корректно работать на мобильных экранах. Не использовать фиксированные ширины в пикселях — предпочитать `%`, `vw`, `auto`, `minmax()`
